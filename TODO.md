# TODO

Replace UUID with session code

Separate presenter and viewer (no hotspots for presenters)

Fix positioning of context boxes

Fix layering order of context boxes (chat should be prioritised, maybe unless the mouse is hovering over the context box)

Weird bug with chat which forces a refresh to see text

Slow load times -> probably just the model being used

Google Drive compat?
